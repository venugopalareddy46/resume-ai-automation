connect ec2

install kubernetes

clone code

go to directory

add health check path app/main.py save

install python apt install python3 -y apt install python3-pip -y apt install python3-venv -y

add venv python3 -m venv .venv source .venv/bin/activate

got to app directory and run add requirements.txt add required liberys

run requirements.txt python3 -m pip install -r requirements.txt


all commands to run cd ~/self-healing-monitor-resume-ai/backend

source .venv/bin/activate

python -m pip install --upgrade pip

python -m pip install -r requirements.txt

python -c "import fastapi; print('FastAPI OK:', fastapi.__version__)"

python -c "from app.main import app; print('Application import OK')"

python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000


application should be running on port 8000 uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 showing browser on port 8000 ec2 public ip:8000 ec2 public ip:8000/docs ec2 public ip:8000/health

run application from ec2 to local machine ssh -i "C:\path\to\your-key.pem" -L 8000:localhost:8000 ubuntu@YOUR_EC2_PUBLIC_IP ---------------------------------------------
